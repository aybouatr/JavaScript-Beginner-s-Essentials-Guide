let name = prompt("Enter your name:", "Guest");


if (name !== null && name !== "")
{
  document.body.innerHTML = `<h2>Welcome, ${name}!</h2>`;
} 
else 
{
  document.body.innerHTML = `<h2>Welcome, ${name}!</h2>`;
}